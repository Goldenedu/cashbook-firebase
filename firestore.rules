rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now
    // You can make this more restrictive later for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
